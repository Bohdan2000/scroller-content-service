networks:
  scroller-net:
    external: true

services:
  content:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: content_service
    restart: unless-stopped
    env_file: .env.staging
    environment:
      NODE_ENV: production
      PORT: 3003
    ports:
      - "3003:3003"
    networks:
      - scroller-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3003/api/v1/health 2>/dev/null || exit 0"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
